export const states = {
    lockScreen: false,
    enablePickColor: false,
    enableFillColor: false,
    eraserEnabled: false,
    matcapUrl: null,
    currentBrush: null,
    currentSelectedObjectId: null,
    selectionMode: false
}

window.states = states;